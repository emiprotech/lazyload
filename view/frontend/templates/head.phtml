<script type="text/javascript">require(['jquery'], function($) {'use strict';var $win = $(window);$.fn.isInViewport = function() {var elementTop = $(this).offset().top;var elementBottom = elementTop + $(this).outerHeight();var viewportTop = $win.scrollTop();var viewportBottom = viewportTop + $win.height();return elementBottom > viewportTop && elementTop < viewportBottom;};var Interval = setInterval(function() {Lazyload();}, 300);setTimeout(function() {clearInterval(Interval);$("img[data-src]").each(function() {var src = $(this).attr("data-src");$(this).attr("src", src).removeAttr("data-src");});$(".lazycss[data-src]").each(function() {var src = $(this).attr("data-src");$(this).css("background",  'url(' + src + ')').removeAttr("data-src");});}, 5000);var scrollTimeout;var throttle = 300;$win.on('resize scroll', function() {if (!scrollTimeout) {scrollTimeout = setTimeout(function () {scrollTimeout = null;Lazyload();}, throttle);}});function Lazyload() {var i = 1;$("img[data-src]").each(function() {if ($(this).isInViewport()) {var src = $(this).attr("data-src");if (src) {$(this).attr("src", src).hide().removeAttr("data-src").fadeIn().removeAttr("style");if(i <= 6){i=0;return false;}i++;}}});var j = 1;$(".lazycss[data-src]").each(function() {if ($(this).isInViewport()) {var src = $(this).attr("data-src");if (src) {$(this).css("background", 'url(' + src + ')').hide().removeAttr("data-src").fadeIn();if(j <= 6){i=0;return false;}j++;}}});}});</script>